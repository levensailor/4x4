# Contributing

Create a new feature branch for each change.

Open a pull request against `main` and describe why the change should be added to the repository. Include the content or app behavior affected, any verification performed, and screenshots when the SwiftUI interface changes.

Do not commit `.env` files, virtual environments, build artifacts, DerivedData, or local Xcode user state.
